<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">MOOCH</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav mr-auto">

        </ul>

        {{#if signedIn}}
        <p id=userId> {{!-- Navbar drop downs --}}
            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">{{email}}
                    <span class="caret"></span>
                </a>
                <ul class="dropdown-menu" style="list-style: none">
                    <li>
                        <a href="#">Profile</a>
                    </li>
                    <li>
                        <a href="#">Transactions</a>
                    </li>
                    <li>
                        <p>
                            <a class="btn btn-primary" href="#">Sign Out</a>
                        </p>
                    </li>
                </ul>
            </li>
        </p>
        {{else}}
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#signInModal" id="signIn">
            Sign-in
        </button>
        <form action="/registration">
            <input type="submit" class="btn" value="Register" />
        </form>
        {{/if}}
    </div>

</nav>


<div class="container">
    <div class="row">
        <div class="col-12-sm">
            <h1 class="text-center">{{firstName}}'s Transactions</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-12-sm">
            <h1 class="text-center">Pending Rental Requests</h1>
        </div>
    </div>
    <div class="row">
        {{#if pendingRequest}}
        <p>Category : {{category}}</p>
        <br>
        <p>Product : {{product_name}}</p>
        <br>
        <p>
            <a class="btn btn-primary" href="">Accept Rental Request</a>
        </p>
        <br>
        <p>You have pending requests</p>
        <br> {{else}}
        <p>YOu have no pending requests</p>
        {{/if}}
    </div>
</div>